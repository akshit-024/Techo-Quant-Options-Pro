"""Normalization and atomic snapshot ingestion.

Concrete acquisition services intentionally remain in their submodules so importing a
domain normalizer never constructs or imports runtime orchestration.
"""
